<%= simple_form_for(@length) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :appointment_length %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
