#profiles.transitions-enabled
  - @profiles.each do |profile|
    .box.panel.panel-default
      = link_to (image_tag profile.avatar.url), profile
      .panel-body
        %h2= link_to profile.username, profile