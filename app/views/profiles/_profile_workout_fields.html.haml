%li.control-group.nested-fields
	.controls
		= f.association :workout, label: "Workout Type", label_method: :workout_name, value_method: :id, include_blank: false, collection: Workout.order('workout_name')
		= f.input :rating, collection: 1..5, label: "Rate how much you love these workouts"
		= link_to_remove_association "Remove Workout Type", f, class: "btn btn-danger"
		%br
		%br
